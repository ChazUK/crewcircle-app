import { View } from "react-native";

import { Title } from "@/components/ui/Title";

export default function DeleteAccount() {
  return (
    <View>
      <Title title="Delete Account" subtitle="Don't go" />
    </View>
  );
}
